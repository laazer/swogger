<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>
    <style name="BasicButton">
        <item name="android:layout_marginTop">@dimen/fragment_margin_top</item>
        <item name="android:textSize">@dimen/lg_button_text_size</item>
        <item name="android:color">@color/black</item>
        <item name="android:height">@dimen/fss_lg_button_hi</item>
        <item name="android:width">@dimen/fss_lg_button_wd</item>
    </style>

</resources>
